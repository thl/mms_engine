<% path = medium_title_translated_title_path(@medium, @title, @translated_title)
   if authorized?(path, :action => 'destroy') %>
<%= link_to ts('delete.this'), path, method: :delete, data: { confirm: te('delete.confirmation') } %> |
<% end %>
<%= render :partial => 'media/back_to_edit', :locals => {:anchor => 'titles'} %>