<%  @medium = @workflow.medium %>
<h1><%= ts :for, :what => Workflow.model_name.human.titleize, :whom => "#{Medium.human_attribute_name(:id)}#{@medium.id}" %></h1>
<p>
    <b><%= Medium.model_name.human.titleize.s %></b><br />    
<%= thumbnail_image_link(@medium) %></td>
</p>
<%= form_for(:status, :url => statuses_path(@statuses)) do |f| %>
<%=   render :partial => 'form', :locals => {:f => f} %>
<%  end %>
<%= render :partial => 'show', :locals => {:workflow => @workflow} %>
<p>
<% path = edit_medium_workflow_path(@medium)
   if authorized? path %>
<%=  link_to ts('edit.this'), path %> |
<% end %>
<%= render :partial => 'persistent_links' %>
</p>
