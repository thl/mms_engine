<h1><%= ts :for, :what => t('edit.ing.record', :what => Picture.human_attribute_name(:rotation)), :whom => "#{Medium.human_attribute_name(:id)}#{@medium.id}" %></h1>
<%= form_for(:medium, :url => medium_rotations_path(@medium)) do |f| %>
<%=   render :partial => 'rotations/form', :locals => {:medium => @medium, :f => f} %>
      <p><%= globalized_submit_tag 'update.this' %></p>
<%  end %>
