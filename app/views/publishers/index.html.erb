<h1><%= ts 'list.ing.records', :what => Publisher.model_name.human(:count => :many).titleize %></h1>
<table border="1" class="normal">
  <tr>
    <th><%= Publisher.model_name.human.titleize.s %></th>
    <th><%= Publisher.human_attribute_name('country_id').s %></th>
  </tr>
<% can_edit = authorized? hash_for_edit_publisher_path
   for publisher in @publishers %>
  <tr>
    <td>
<%=   link_to h(publisher.title), can_edit ? edit_publisher_path(publisher) : publisher_path(publisher) %>
	</td>
    <td><%=h(publisher.country.header) if !publisher.country.nil? %></td>
  </tr>
<% end %>
</table>
<p>&nbsp;<br/>
<% if authorized? hash_for_new_publisher_path %>
<%=  link_to ts('new.record', :what => Publisher.model_name.human), new_publisher_path %> |
<% end %>
<%= render :partial => 'main/back_to_admin_or_media' %>
</p>
