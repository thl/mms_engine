<h1><%= ts(:for, :what => t('list.ing.records', :what => Citation.human_name(:count => :many).capitalize), :whom => @reference.title) %></h1>

<% form_for(:citation, :url => @reference.class == Title ? medium_title_citations_path(@medium, @reference, @citation) : medium_title_translated_title_citation_path(@medium, @title, @reference, @citation)) do |f| %>
<%=  render :partial => 'form', :locals => {:f => f } %>
     <p><%= globalized_submit_tag 'create.this' %></p>
<% end %>
<p>&nbsp;<br/>
<%= render :partial => 'persistent_links' %></p>