<% path = polymorphic_path(@reference_stack + [@citation])
   if authorized?(path, :action => 'destroy') %>
<%= link_to 'Delete', path, method: :delete, data: { confirm: 'Are you sure?' } %> |
<% end %>
<%= render :partial => 'media/back_to_edit', :locals => {:anchor => 'titles'} %>
