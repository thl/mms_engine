<h1><%= ts :for, :what => t('edit.ing.record', :what => Description.human_name.titleize), :whom => "#{Medium.human_name.titleize} #{@medium.id}" %></h1>
<%= error_messages_for :description %>
<%  form_for(:description, :url => medium_description_path(@medium, @description), :html => { :method => :put }) do |f| %>
<%=   render :partial => 'form', :locals => {:f => f} %>
      <p><%= globalized_submit_tag 'update.this' %></p>
<%  end %>
<p>&nbsp;<br/>
<%= render :partial => 'persistent_links' %></p>