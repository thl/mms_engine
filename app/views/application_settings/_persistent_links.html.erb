<%  path = application_setting_path(@application_setting)
    if authorized?(path, :action => 'destroy') %>
<%=   link_to ts('delete.this'), path, method: :delete, data: { confirm: te('delete.confirmation') } %> |
<%  end %>
<%= link_to ts('back.to', :what => ApplicationSetting.model_name.human(:count => :many).titleize), application_settings_path %> |
<%= render :partial => 'main/back_to_admin_or_media' %>