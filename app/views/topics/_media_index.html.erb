<h2>Media Associated with <%= @topic.title %></h2>
<% if !@pictures.nil? || !@videos.nil? || !@documents.nil? %>
<%=  render :partial => 'media/index' %>
<% elsif !@media.nil? %>
<%=  render :partial => 'media/paged_index' %>
<% end %>
<p><%= link_to ts(:in, :what => @topic.title, :where => 'Topics'), Category.element_url(@topic.id, :format => 'html') %></p>