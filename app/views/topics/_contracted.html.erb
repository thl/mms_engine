<%= (Util::MARGIN * margin_depth).html_safe %>
<%  if contracted.is_childless?
	  (Util::MARGIN * margin_depth).html_safe %>
	  &#9674;
<%	else %>
<%=   link_to '+', expand_topic_path(contracted.id, :margin_depth => margin_depth), :remote => true, :class => 'collapsable' %>
<%  end %>
<%= link_to h(contracted.header).s, topic_path(contracted.id, :margin_depth => margin_depth), :remote => true %> (<%= number_with_delimiter contracted.media_count, :delimiter => ',' %>)<br/>