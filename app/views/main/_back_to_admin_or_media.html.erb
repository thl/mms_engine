<%  path = admin_path %>
<%= authorized?(path) ? link_to(ts('back.to', :what => t('admin.page')), path) : link_to(ts('back.to', :what => t('all.records', :what => Medium.model_name.human(:count => :many))), media_path) %>