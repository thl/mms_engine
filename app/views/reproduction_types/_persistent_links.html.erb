<%  path = reproduction_type_path(@reproduction_type)
    if authorized?(path, :action => 'destroy') %>
<%=   link_to ts('delete.this'), path, method: :delete, data: { confirm: te('delete.confirmation') } %> |
<%  end %>
<%= link_to ts('back.to', :what => t('all.records', :what => ReproductionType.model_name.human(:count => :many))), reproduction_types_path %> |
<%= render :partial => 'main/back_to_admin_or_media' %>