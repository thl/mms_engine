<%  path = capture_device_maker_model_path(@capture_device_maker, @capture_device_model)
    if authorized?(path, :action => 'destroy') %>
<%=   link_to ts('delete.this'), path, method: :delete, data: { confirm: te('delete.confirmation') } %> |
<%  end %>
<%= link_to ts('back.to', :what => ts('all.records', :what => CaptureDeviceMaker.model_name.human(:count => :many))), capture_device_makers_path %> |
<%= render :partial => 'main/back_to_admin_or_media' %>